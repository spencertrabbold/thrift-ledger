{
  "name": "thrift-ledger-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for Thrift Ledger (SPE-25) — detached AI listing-generation jobs.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
